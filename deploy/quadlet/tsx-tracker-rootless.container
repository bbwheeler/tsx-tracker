[Container]
Image=localhost/tsx-tracker:latest
Network=host
EnvironmentFile=%h/.config/tsx-tracker/.env.podman
PodmanArgs=--read-only
PodmanArgs=--tmpfs=/tmp:rw,noexec,nosuid,size=64m

[Service]
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
